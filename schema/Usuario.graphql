enum Status {
  ATIVO
  INATIVO
  BLOQUEADO
}

type Usuario {
  id: Int
  nome: String
  email: String
  salario: Float
  vip: Boolean
  perfil: Perfil
  status: Status
}

type Perfil {
  id: Int
  nome: String
}
