scalar Data

type Query {
  ola: String!
  horaAtual: String
  dataAtual: Data
  melhorUsuario: Usuario!
  melhorProduto: Produto!
  numerosMegaSena: [Int!]!
  usuarios: [Usuario!]!
  usuario(id: Int): Usuario
  perfis: [Perfil!]!
  perfil(id: Int): Perfil
}
